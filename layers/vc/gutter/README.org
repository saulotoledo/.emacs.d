#+TITLE: VC Gutter Layer
#+CATEGORY: vc
#+PROPERTY: header-args:emacs-lisp :tangle yes

* Description
Displays git diff indicators in the fringe using diff-hl and inline git blame
annotations using blamer.el.

* Emacs Configuration
** Diff-HL Fringe Indicators
#+begin_src emacs-lisp
(use-package diff-hl
  :defer t
  :hook ((magit-pre-refresh . ignore)
         (magit-post-refresh . diff-hl-magit-post-refresh))
  :config
  (global-diff-hl-mode 1))
#+end_src

** Blamer Inline Annotations
#+begin_src emacs-lisp
(defun cfg-vc/toggle-blamer ()
  "Toggle global-blamer-mode on and off."
  (interactive)
  (global-blamer-mode 'toggle))

(use-package blamer
  :demand t
  :custom
  (blamer-type 'both)
  (blamer-idle-time 0.3)
  (blamer-overlay-priority 1)
  :custom-face
  (blamer-face ((t :foreground "#5c6370"
                   :background unspecified
                   :height 85))))
#+end_src

* Keybindings
#+begin_src emacs-lisp
(cfg-keys/leader-def
  "g"     '(:ignore t :which-key "git")
  "g b"   '(:ignore t :which-key "blamer")
  "g b i" '(blamer-show-commit-info :which-key "Show Commit Info")
  "g b i" '(blamer-show-posframe-commit-info :which-key "Show Commit Info in Posframe")
  "t"     '(:ignore t :which-key "toggles")
  "t b"   '(cfg-vc/toggle-blamer :which-key "Blamer Inline Annotations"))
#+end_src

* System Setup
No system setup required.
